from datetime import datetime
from .constants import CATEGORIES

CATEGORY = CATEGORIES.POST
AUTHOR = "Pablo"
POST_TITLE = "JS/CSS workflow using Bundler, Bower and Guard in a Django project"
POST_DESCRIPTION = "A simple example of a js and css workflow. It uses tools like" \
                   " Bundler, Bower, Guard inside a django project that includes a pipeline"

POST_TAGS = ("Javascript", "CSS", "workflow")

PUBLISHED_DATE = datetime(year=2014, month=3, day=7)
